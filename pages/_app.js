// Import the global styles and Tailwind CSS styles
import '../src/styles/globals.css';
import 'tailwindcss/tailwind.css';

function ChatApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default ChatApp;
